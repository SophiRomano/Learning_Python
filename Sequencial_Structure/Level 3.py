# Make a progam that print the sum of two randoms numbers

import random
print(random.randint(0,9)+random.randint(0,9))      # This is the easiest way to do that, 
                                                    # but we can't confirm the result
# Other way to know if the results are write

a = random.randint(0,9)
b = random.randint(0,9)

print(a)
print(b)
print(a+b)